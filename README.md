# Projeto de livraria do curso de api rest da alura

uso de 
> npm install nodemon


> npm isntall express


e adicionar nas configuracoes do package.json

> "type": "module",


>"scripts": {
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
